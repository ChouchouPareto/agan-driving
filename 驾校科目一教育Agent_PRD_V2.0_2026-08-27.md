# 驾校科目一教育 Agent PRD V2.0

> 文档状态：方案待确认，尚未进入开发  
> 版本日期：2026-08-27  
> 产品阶段：MVP  
> 首发载体：自建 Web  
> 适用流程：焚诀 Agent Blueprint 五步 SOP  
> 开发基线：development/stage-2a-2026-08-26，commit 29ae01b

## 0. 文档结论

本版本将项目从“能回答问题的驾校智能客服”升级为“围绕学员理解结果闭环的科目一教育 Agent”。

核心公式：

- 教育 Agent = 模型 + 课程知识 + 学生进度 + 评估工具
- Agent = 模型 + Harness
- Harness = 工具 + 知识 + 观察 + 行动接口 + 权限

产品不是单纯聊天窗口，也不是通用刷题 App。它由驾校交付给学员，优先解决学员在真实学习现场遇到的科目一疑问；系统利用题库、法规和驾校资料提供可追溯答案，必要时通过“不懂就问校长”转入人工闭环。

北极星指标：有效解决率。

有效解决率 = 有可靠证据、完成回答且获得理解反馈或人工闭环的问题数 ÷ 已进入处理的问题总数。

“已生成回答”不等于“已解决”，“看懂了”只能作为一次弱证据，不能直接判定知识点掌握。

---

## 1. Step 1：需求硬事实与风险识别

### 1.1 已确认事实

| 项目 | 已确认内容 |
|---|---|
| 核心用户 | 驾校科目一学员；协作角色为校长、教练、知识审核员 |
| 核心场景 | 学员通过文字或题目截图提问，获得标准答案、依据与易懂解释 |
| 产品入口 | MVP 先搭建自有 Web，后续再评估接入驾校小程序、SaaS 或 ERP |
| 业务模式 | 由驾校交付给学员，属于 B2B2C 服务 |
| 首要目标 | 回答正确，并进一步验证问题是否真正解决 |
| 内容范围 | MVP 聚焦科目一；科目四、科目二/三咨询后续评估 |
| 答案质量 | 机器处理为主，异常、争议和低置信度场景由人工审核 |
| 知识来源 | 驾校已有明确资料供应方，正式资料后续补充；MVP 可先自行整理 |
| 模型现状 | 已配置 DeepSeek 与阿里云百炼；OCR 已验证 qwen-vl-ocr |
| 当前基线 | 第二阶段 2A 已完成基础问答工作区与前后端代理 |
| 试点规模 | 预计 30–50 名学员、4 周 |
| 数据策略 | 图片暂定保存 7 天；会话暂定保存 180 天；试点结束 30 天后删除或匿名化学习记忆 |

### 1.2 核心闭环

学员提问 → 识别题目 → 匹配标准题与知识证据 → 生成教学解释 → 学员反馈是否理解 → 未理解则换一种解释 → 仍未解决或存在风险则“不懂就问校长” → 校长回复 → 沉淀学习证据与知识改进线索。

### 1.3 明确边界

1. 标准题答案不得由模型自由改写结论。
2. 没有可靠证据、证据冲突或超出范围时，必须拒答、降级或转人工。
3. OCR 低置信度字段必须让学员确认，不能静默猜测。
4. AI 不得自动发布知识、关闭争议工单或修改正式题库。
5. 不向学员展示容易误导的“答案置信度百分比”。
6. 身份证、财务、缴费等敏感信息不得进入问答模型。
7. 不提供替代线下教练判断的真实驾驶操作指令。
8. 本版本不建设小程序、ERP 深度集成、提醒系统、独立学习看板、CSV 批量导入和用户自助上传评测集。
9. PRD 未确认前，不进入下一阶段编码。

### 1.4 八问风险检查

| 问题 | 当前答案 |
|---|---|
| 目标是否清晰 | 是：可靠回答并完成理解闭环 |
| 用户是否清晰 | 是：学员为主，校长/教练和知识审核员协作 |
| 输入输出是否清晰 | 是：文字/截图输入，结构化答案、依据、解释和处理状态输出 |
| 知识是否可信 | 部分明确；供应商已确认，但正式资料、授权范围待补充 |
| 工具是否可控 | 是；所有副作用工具需鉴权、幂等、审计 |
| 权限是否清晰 | 形成初版角色矩阵，具体账号来源与排班待确认 |
| 异常是否可恢复 | 方案已覆盖 OCR、模型、检索、网络和人工 SLA 降级 |
| 验收是否可量化 | 已定义指标与分阶段验收，阈值仍需试点评测确认 |

### 1.5 待确认的高风险事实

- 试点地区、准驾车型及适用法规版本。
- 供应商资料的交付格式、更新频率、版权和最终审核责任人。
- 校长/教练的登录方式、值班安排、服务时段与 SLA。
- 单次问题、单学员和试点总成本预算。
- 正式域名、部署云、备案与隐私合规负责人。

---

## 2. Step 2：Harness 五要素

### 2.1 工具

- 图片上传与安全校验
- OCR 识别与结构化
- OCR 字段确认
- 标准题匹配
- 课程知识检索与重排
- 教学解释生成
- 答案质量评估
- 学习证据记录
- 人工工单创建、领取、回复和关闭
- 知识草稿、审核、发布、下线
- 离线评测集运行与结果阻断

### 2.2 知识

- 标准题、标准答案与解析
- 交通法规、地方规则和版本信息
- 驾校教学资料与知识卡片
- 易错点和误解类型标签
- 面向初学者的解释模板、反例和记忆提示
- 必须转人工的场景与安全边界

### 2.3 观察

- OCR 原文、字段置信度和学员修正记录
- 检索命中、引用证据、题库版本和冲突信息
- 模型、提示词、工作流、知识版本
- 学员反馈、二次解释、工单结果
- 离线评测结果、错误类型、延迟、Token 和成本

### 2.4 行动接口

- 学员 Web 问答与状态查询
- 管理端工单、知识和评测操作
- 同步 API、流式响应、异步任务提交与查询
- 校长人工回复
- 知识发布与下线

### 2.5 权限

| 角色 | 可执行动作 | 禁止动作 |
|---|---|---|
| 学员 | 管理自己的会话、图片、反馈和工单 | 查看他人数据、发布知识 |
| 校长/教练 | 查看授权范围内工单、领取、回复 | 直接发布正式知识、查看无关敏感数据 |
| 知识管理员 | 建草稿、关联证据、提交审核 | 单人绕过审核发布高风险内容 |
| 专业审核员 | 审核、发布、下线、处理争议 | 无审计修改线上记录 |
| 系统管理员 | 账号、权限、配置和审计 | 以系统权限替代专业内容审核 |

所有写操作必须记录操作者、时间、对象、前后版本和 requestId。

---

## 3. Step 3：最小组件选择

### 3.1 本阶段采用

| 组件 | 用途 | 缺失后果 |
|---|---|---|
| s01 Agent Loop | 驱动识别、检索、回答、反馈和升级闭环 | 系统退化为一次性聊天 |
| s02 Tools | 统一 OCR、检索、工单等工具契约 | 模型与业务副作用耦合 |
| s03 Permissions | 按角色限制数据与动作 | 发生越权和错误发布 |
| s04 Hooks | 在前后置节点执行校验、审计和指标 | 关键规则散落且难追踪 |
| s07 Skills/Knowledge | 按需加载题库、法规、解释规范 | 上下文膨胀且证据混乱 |
| s09 Memory | 保存最小学习证据 | 无法支持连续学习与误区识别 |
| s11 Background Tasks | 承载 OCR、评测和慢模型任务 | 长任务阻塞请求且不可恢复 |
| s15 Integrated Harness | 将工具、知识、观察、权限统一编排 | 无法形成可运营 Agent |
| s16 Workflows | 固化 OCR 确认、知识发布等高风险流程 | 高风险步骤被模型自由决定 |
| s17 Goal Loop | 以“是否解决”为终止条件 | 回答生成后过早结束 |

### 3.2 暂缓

- s05 Task Planning：科目一单问题链路较短，暂不需要复杂规划器。
- s08 Context Compression：MVP 会话受控，达到真实上下文压力后再引入。
- s10 Generic Task System：先用专用异步任务表，避免过度抽象。

### 3.3 不采用

- s06 Subagents、s13 Agent Teams：MVP 不需要多 Agent 分工。
- s12 Scheduler：当前不做提醒和学习计划推送。
- s14 MCP：现有内部 API 足够，不为协议而增加复杂度。

原则：只选择能直接降低当前产品风险的组件。

---

## 4. Step 4：完整产品方案

### 4.1 七层架构

    1. 交互层
       学员 Web ｜ 校长/知识管理 Web
                         ↓
    2. 产品功能层
       会话问答 ｜ OCR确认 ｜ 学习证据 ｜ 工单 ｜ 知识 ｜ 评测
                         ↓
    3. Agent 编排层
       Loop ｜ Tools ｜ Permissions ｜ Hooks ｜ Knowledge
       Memory ｜ Background Tasks ｜ Goal Loop ｜ Fixed Workflows
                         ↓
    4. 模型层
       qwen-vl-ocr ｜ qwen3.7-plus ｜ qwen3.6-flash ｜ 备用模型/人工
                         ↓
    5. 集成层
       标准题库 ｜ RAG/Embedding/Rerank ｜ 百炼 ｜ 对象存储 ｜ 工单服务
                         ↓
    6. 数据层
       学员/会话/图片/OCR/答案/学习证据/工单/知识/评测/Trace
                         ↓
    7. 基础设施层
       Next.js ｜ FastAPI ｜ PostgreSQL ｜ Worker/Queue ｜ 国内云 ｜ 监控

安全、权限、审计和可观测性横跨全部七层。

### 4.2 主数据流

1. 学员提交文字或图片，服务端进行身份、文件和内容校验。
2. 图片进入对象存储并创建 OCR 异步任务。
3. OCR 输出题干、选项和低置信字段；必要时要求学员确认。
4. 系统匹配标准题，再检索法规和驾校知识，保留证据与版本。
5. 若标准题命中，标准答案作为不可变结论；模型只负责教学解释。
6. 质量评估检查证据一致性、范围、安全和格式。
7. 前端展示答案、依据、解释及“看懂了/还是不懂”。
8. 未理解时生成不同解释；仍未解决、无证据或有争议则转人工。
9. 全链路写入学习证据、工单、审计、Trace 和产品指标。

### 4.3 工具契约

| 工具 | 输入 | 输出 | 副作用 | 权限/约束 |
|---|---|---|---|---|
| upload_question_image | 文件、会话ID | assetId、校验结果 | 写对象存储 | 学员本人；类型/大小/恶意文件校验 |
| extract_question_ocr | assetId | 题干、选项、置信字段 | 写 OCR 记录 | 系统；异步、可重试 |
| confirm_ocr_fields | ocrId、修正字段 | 已确认结构 | 更新 OCR 版本 | 学员本人；保留前后值 |
| match_standard_question | 结构化题目、地区、车型、版本 | questionId、匹配分数 | 无 | 仅查询已发布题库 |
| retrieve_course_knowledge | 查询、过滤条件 | 证据片段、来源、版本 | 无 | 仅返回已发布知识 |
| generate_teaching_explanation | 标准答案、证据、学习上下文 | 解释、引用、状态 | 写回答草稿 | 不得改变标准结论 |
| evaluate_answer_quality | 问题、答案、证据、版本 | 通过/阻断、错误标签 | 写评估记录 | P0 错误必须阻断 |
| record_learning_evidence | 学员、知识点、结果、来源 | evidenceId | 追加学习证据 | 不直接改“已掌握” |
| create_review_ticket | 问题、原因、上下文摘要 | ticketId、状态 | 创建工单 | 脱敏；幂等 |
| claim_review_ticket | ticketId、操作者 | 处理中状态 | 更新领取人与时间 | 校长/教练 |
| reply_review_ticket | ticketId、回复、证据 | 已回复状态 | 写人工回复 | 授权校长/教练 |
| draft_knowledge_revision | 来源、修改内容 | draftId | 建知识草稿 | 知识管理员 |
| publish_knowledge_revision | draftId、审核结论 | 新版本 | 发布/下线知识 | 专业审核员；双重审计 |
| run_evaluation_suite | 评测集、模型/知识版本 | 指标、错误明细 | 写评测运行 | 管理员；可异步 |

通用要求：输入输出使用版本化 Schema；写操作支持幂等键；超时、重试和错误码明确；工具不得把密钥、原始敏感数据放入日志。

### 4.4 教学目标循环

    待处理
      → 已形成可靠答案
      → 等待学员反馈
          → 看懂了：记一次弱理解证据，问题结束
          → 还是不懂：使用另一种解释策略
              → 看懂了：记录二次解释有效
              → 仍不懂/有争议：提交给校长
                  → 人工回复并确认闭环

单次“看懂了”不能推导为知识点已掌握；掌握状态只能由多次题目表现、时间跨度和评估工具综合推导。

### 4.5 系统提示词规范

身份：你是由驾校交付给学员的科目一教育助教，目标是用可靠证据帮助学员理解，而不是追求回答数量。

硬规则：

1. 优先匹配标准题，标准答案不可改写。
2. 所有事实性结论必须来自已发布且适用的证据。
3. 无证据、证据冲突、版本不明或超出科目一范围时，不猜测。
4. OCR 不确定时先让学员确认。
5. 首次解释简短直接；学员不懂时更换角度，不机械复述。
6. 需要人工时说明原因，并调用工单工具。
7. 不收集或输出身份证、财务、缴费等敏感信息。
8. 不将一次反馈描述成“已经掌握”。

回答结构：结论 → 为什么 → 依据 → 易错提醒 → 理解反馈入口。

完成条件：获得有效理解反馈，或人工工单完成回复并进入闭环。

降级条件：模型不可用、证据不足、知识冲突、OCR 失败、权限不足或高风险内容。

### 4.6 上下文与记忆

会话内保存：

- 当前题目、OCR 确认结果、证据 ID 和版本
- 最多两次不同解释及学员反馈
- 工单 ID 和状态

跨会话只保存最小学习证据：

- 知识点、是否解决、尝试次数、时间、来源题目
- 常见误区标签和有效解释方式

记忆规则：

- 证据追加，不覆盖历史事实。
- 引用保存 ID、版本和摘要，不把整份资料塞入提示词。
- 不保存身份证、财务、缴费和无关聊天。
- 学员可按规则删除；试点结束后执行删除或匿名化。
- “学习状态”是可重新计算的派生结果，不是模型自由填写的事实。

### 4.7 安全与降级

| 场景 | 系统行为 | 用户可见结果 |
|---|---|---|
| 图片格式或大小非法 | 上传前后双重校验并拒绝 | 明确说明支持格式与限制 |
| OCR 失败 | 自动有限重试，保留文字输入入口 | 请重新拍摄或手动输入 |
| OCR 低置信 | 高亮字段并要求确认 | 确认题干/选项后继续 |
| 未匹配标准题 | 进入证据检索，不得伪装命中 | 说明正在按知识资料查找 |
| 无证据或证据冲突 | 阻断生成，转人工 | “这题需要校长确认” |
| 模型超时 | 切换备用模型或转异步任务 | 显示处理中，可稍后回来 |
| 刷新或断网 | 使用 requestId 恢复状态 | 不重复提交、不丢任务 |
| 越权访问 | 拒绝并记安全审计 | 无权限提示 |
| P0 评测错误 | 阻断版本发布 | 管理端展示失败原因 |
| 人工超 SLA | 自动提醒并升级责任人 | 显示预计处理时间和状态 |

### 4.8 可观测性

每次请求应记录：

- requestId、sessionId、questionId、assetId、taskId、ticketId
- 模型、提示词、工作流、知识和评测版本
- 工具调用顺序、耗时、重试、错误码
- 检索命中与证据 ID
- Token、模型费用和存储费用
- 质量评估结果与最终闭环状态

日志不得记录 API 密钥、完整敏感字段和无必要的原始图片。

指标分组：

| 分组 | 指标 |
|---|---|
| 结果 | 有效解决率、人工闭环率、问题放弃率 |
| 质量 | 标准题正确率、证据支持率、幻觉率、P0 错误数 |
| 教学 | 首次看懂率、二次解释有效率、重复误区率 |
| 效率 | 首次响应时间、完整答案时间、人工 SLA |
| 系统 | OCR 成功率、任务成功率、恢复率、错误率 |
| 成本 | 单问题模型成本、单学员成本、人工处理成本 |

### 4.9 技术栈与部署约束

| 层级 | MVP 方案 |
|---|---|
| 前端 | Next.js 16.3.3、React 19、TypeScript strict、CSS Design Tokens |
| 后端 | FastAPI、Pydantic、SQLAlchemy、Alembic |
| 数据库 | 本地 SQLite；试点 PostgreSQL |
| 文件 | 开发期本地；试点使用私有对象存储与时效 URL |
| 异步任务 | 持久化任务表 + Worker；规模增长后再评估专用队列 |
| 模型 | qwen-vl-ocr；qwen3.7-plus 主推理；qwen3.6-flash 轻任务；DeepSeek 作为可评测备选 |
| RAG | Embedding + 混合检索 + Rerank + 来源版本过滤 |
| 编排 | 先由业务后端实现固定流程；Dify 是否自部署待确认 |
| 鉴权 | BFF + HttpOnly Cookie；服务端密钥不下发浏览器 |
| 测试 | pytest、Vitest、Testing Library、MSW、Playwright |
| 部署 | 国内云、域名、备案、监控和告警方案待确认 |

---

## 5. 产品功能要求

### 5.1 学员端页面

| 页面 | 主要能力 |
|---|---|
| /enter | 驾校邀请码进入；展示必要隐私说明 |
| /ask | 文字提问、图片上传、OCR 校正、任务恢复 |
| /questions/[id] | 展示结论、依据、解释、反馈和处理状态 |
| /tickets/[id] | 展示“不懂就问校长”的状态与人工回复 |

本期不建设独立学习进度页。学生进度先作为 Agent 内部学习证据，用于解释策略和产品评估；是否对学员可见待试点后决定。

### 5.2 OCR 与图片

- 支持 JPEG、PNG、WebP；一次一张；上限暂定 10MB。
- 前端校验只用于体验，服务端必须重新验证 MIME、文件头和大小。
- 文件随机命名，不使用用户原文件名作为公开地址。
- 对象默认不可公开访问，访问使用短时签名。
- OCR 输出题干、选项和置信字段；低置信字段必须确认。
- 原图默认 7 天删除，删除任务及结果需审计。

### 5.3 答案卡

必须包含：

- 明确结论
- 适用题目/地区/车型/版本
- 简短原因
- 可追溯依据
- 易错提醒
- “看懂了”“还是不懂”“不懂就问校长”

标准题命中时，结论取自标准答案；模型只生成教学解释。未命中或冲突时不得输出伪确定答案。

### 5.4 不懂就问校长

用户可见状态固定为：

提交给校长 → 校长在摸鱼 → 校长处理中 → 校长已回复 → 校长说好了

其中“校长在摸鱼”是产品化文案，界面需同时显示正式说明，例如“已进入等待队列，预计在服务时段内处理”，避免误解。

状态要求：

- 创建工单后可刷新恢复。
- 校长领取后进入“校长处理中”。
- 回复后学员可查看并确认。
- 学员确认或按规则完成后进入“校长说好了”。
- 工单不可由 AI 自动关闭。

### 5.5 校长工作台

- 查看待领取、处理中、已回复工单。
- 按驾校、时间、风险和超时排序。
- 查看脱敏后的问题、OCR、证据、AI 回答和转人工原因。
- 领取、回复、补充依据、提交知识改进建议。
- 所有状态变更具备幂等与审计。

账号来源、工单可见范围、服务时间、SLA 和升级人待确认。

### 5.6 知识与评测

- 知识状态：草稿、待审核、已发布、已下线。
- 正式知识必须包含来源、地区、车型、生效时间和版本。
- 高风险变更至少由专业审核员发布。
- 发布前运行固定评测集；P0 错误阻断发布。
- 线上反馈只形成候选样本，不自动成为标准答案。
- 模型、提示词、知识或工作流变化均需形成新评测记录。

### 5.7 UI 与交互

- 第一版视觉沿用已确认的 Loom 风格基线：克制留白、浅色背景、圆角卡片、紫色主色、清晰层级和轻量动效。
- 移动优先，同时支持桌面 Web。
- 异步操作必须展示排队、处理中、成功、失败和可重试状态。
- 禁止只有颜色的状态表达；按钮具备键盘焦点和可读标签。
- 动效遵守减少动态效果设置，不影响任务完成。

---

## 6. 数据模型

| 实体 | 关键内容 |
|---|---|
| StudentSession | 驾校、学员匿名标识、会话、过期时间 |
| Question | 文本/OCR、地区、车型、知识点、处理状态 |
| Asset/OCRResult | 私有文件、保留期、结构化字段、确认版本 |
| Answer | 标准结论、解释、证据、模型和版本、质量状态 |
| Feedback | 看懂/不懂、解释轮次、时间 |
| LearningEvidence | 知识点、结果、尝试、来源、有效期 |
| ReviewTicket | 原因、状态、领取人、回复、SLA |
| KnowledgeItem | 内容、来源、范围、版本、审核和状态 |
| EvaluationRun | 数据集、模型/知识/工作流版本、指标、错误 |
| Trace/AuditLog | 请求链路、工具调用、权限和变更记录 |

数据表必须具备 createdAt、updatedAt；可变业务对象具备 version；外部重试写入具备 idempotencyKey。

---

## 7. 版本范围与开发顺序

### 7.1 已完成：2A 基础问答骨架

- 邀请码入口
- 问答工作区
- 前后端代理与基础 API
- 基础会话和错误状态

### 7.2 下一候选切片：2B OCR 闭环

- 安全图片上传
- qwen-vl-ocr 异步调用
- 结构化题干与选项
- 低置信字段确认
- 标准题匹配接口
- 刷新恢复、重试、审计和测试

### 7.3 后续切片

- 2C：校长工作台与完整工单状态。
- 2D：知识版本、审核发布、离线评测和发布门禁。
- 2E：最小学习证据、解释策略和目标闭环；不建设独立学习页。

每个切片都遵循：骨架 → 加固 → 产品化 → 验收。上一切片验收通过后再进入下一切片。

---

## 8. 验收标准

### 8.1 2B OCR

- 合法图片能上传，非法类型、超限和伪造 MIME 被服务端拒绝。
- OCR 能输出题干与选项；低置信字段可确认并保留版本。
- OCR 超时、失败和刷新均能恢复，不产生重复任务。
- 原图私有存储并按保留期删除。
- 测试覆盖成功、失败、重试、幂等、越权和删除。

### 8.2 2C 工单

- 五个用户可见状态正确流转。
- 校长只能查看授权范围内工单。
- 领取、回复和关闭操作可审计、可重试且不重复。
- 超 SLA 能提醒和升级。

### 8.3 2D 知识与评测

- 每条正式知识可追溯来源、范围、时间和审核人。
- 未发布知识不会进入线上检索。
- P0 错误可阻断发布。
- 模型、提示词、工作流和知识版本能关联评测结果。

### 8.4 2E 教学闭环

- 首次未理解后会切换解释策略，而非原文复述。
- 单次反馈只记录弱证据，不直接标记掌握。
- 同一问题可追踪到最终理解或人工闭环。
- 学员可按规则删除学习证据。

### 8.5 生产级总验收

| 维度 | 通过条件 |
|---|---|
| 可运行 | 从邀请进入到答疑/转人工主链路可完成 |
| 可容错 | 模型、OCR、检索、网络失败均有恢复或降级 |
| 可观测 | 请求、工具、版本、成本和闭环状态可追踪 |
| 可测试 | 单元、契约、集成、E2E 和离线评测可重复运行 |
| 可部署 | 环境变量、迁移、Worker、存储和回滚说明完整 |
| 可运营 | 工单、知识、评测和核心指标具备管理入口 |
| 可衡量 | 能计算有效解决率及其质量、教学、效率和成本分解 |

---

## 9. 优先级

### P0

- 标准题与证据约束
- OCR 确认
- 安全上传
- 工单人工闭环
- 权限、审计、幂等和恢复
- 知识版本与 P0 评测阻断
- 有效解决率数据链路

### P1

- 二次解释策略
- 学习证据与误区标签
- 校长 SLA 与升级
- 成本与质量仪表
- 多模型评测与降级

### P2

- 小程序入口
- ERP/SaaS 学员档案集成
- 科目四及科目二/三咨询
- 学员学习看板和提醒
- 更复杂的个性化学习路径

---

## 10. 待确认决策

1. 试点地区、准驾车型和规则版本。
2. 供应商资料的授权、格式、更新机制和审核责任人。
3. 校长/教练登录方式、工单范围、排班、SLA 和升级机制。
4. 单问题、单学员和试点总成本上限。
5. Dify 自部署还是继续由业务后端编排。
6. 评测集负责人、错误分级和正式上线阈值。
7. 学习证据是否在试点期向学员展示。
8. 试点日期、域名、品牌名称、部署云和数据删除负责人。

以上未确认项不得由开发阶段自行假设为正式业务规则。

---

## 11. Step 5：开发闸门

本 PRD 当前停留在方案确认阶段。确认后才允许进入 2B，顺序为：

2B 骨架 → 2B 加固 → 2B 产品化 → 2B 验收 → 2C → 2D → 2E。

任何阶段若发生以下变化，必须回到方案层更新本文：

- 用户、目标或北极星指标变化
- 新增高风险工具或外部副作用
- 权限、知识来源、模型或合规边界变化
- 从 Web 扩展到小程序、ERP 或 SaaS
- 从科目一扩展到其他科目

---

## 12. 相比 V1.0 的关键变化

1. 产品定位从智能客服升级为科目一教育 Agent。
2. 将学生进度定义为最小学习证据，而非立即建设学习页面。
3. 将“回答正确”扩展为“可靠回答 + 理解反馈 + 必要时人工闭环”。
4. 新增 Harness 五要素、组件选择依据和七层架构。
5. 新增版本化工具契约、系统提示词、记忆、权限和安全降级。
6. 明确标准答案不可由模型自由生成，证据不足必须阻断。
7. 新增知识审核、离线评测和 P0 发布门禁。
8. 将异步任务、幂等、恢复、可观测和成本纳入产品验收。
9. 保留“不懂就问校长”及五段状态文案，并补充正式 SLA 表达。
10. 建立逐切片确认闸门，PRD 未确认前不进入开发。

---

文档结论：V2.0 已完成 Step 1–4 的产品方案整理，等待产品负责人确认。当前不启动 2B 开发。
